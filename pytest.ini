# Issue #20: Point pytest's on-disk cache outside the checkout to avoid the
# "permission denied on .pytest_cache\v\cache\nodeids" Windows noise that
# survived the earlier tmp_path fix. The cache itself is harmless, but the
# noisy warning was non-reproducible across developers/CI runners.
[pytest]
cache_dir = .pytest_tmp/cache
